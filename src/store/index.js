import { createStore } from "vuex";

export default createStore({
  state: {
    isAuthenticated: true,
    user: {
      type: "standard",
    },
  },
  mutations: {},
  actions: {},
  modules: {},
});
